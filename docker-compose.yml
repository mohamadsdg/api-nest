version: '3'

services:
  db:
    image: mongo
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGODB_DATABASE: nest-course
      # MONGO_INITDB_ROOT_USERNAME: admin
      # MONGO_INITDB_ROOT_PASSWORD: pass123
  # mongo-express:
  #   image: mongo-express
  #   restart: always
  #   depends_on:
  #     - db
  #   ports:
  #     - 8081:8081
  #   environment:
  #     ME_CONFIG_MONGODB_ENABLE_ADMIN: 'false'
  #     ME_CONFIG_MONGODB_AUTH_DATABASE: db
  #     ME_CONFIG_MONGODB_AUTH_USERNAME: admin
  #     ME_CONFIG_MONGODB_AUTH_PASSWORD: pass123
